# Bun 配置文件
# https://bun.sh/docs/runtime/bunfig

[install]
# 使用 npm 兼容模式
registry = "https://registry.npmmirror.com/"
cache = "~/.bun/install/cache"

[install.lockfile]
# 保存 lockfile
save = true
print = "yarn"
